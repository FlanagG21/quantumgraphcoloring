name: test
channels:

  - defaults
  - conda-forge
dependencies:
    # Base depends
  - python
  - pip

  # Project depends
  - matplotlib
  - numpy
  - networkx
  - qiskit
  - qiskit-aer
    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

